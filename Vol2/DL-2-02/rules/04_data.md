# Tables, formulas, and verification

- A total is a formula, never a typed number.
- An unknown cell stays empty and the reason goes in the notes column.
- Cross-check the row count against the per-file counts and report both.
- Do not reorder rows unless asked. Order carries meaning in an audit table.
